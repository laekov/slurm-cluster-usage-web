clusters = {
    'name of a cluster': 'hostname of the headnode',
    'name of another cluster': 'hostname of that headnode',
}
days = [7, 30, 365]
outdir = "somewhere to write the webpages"
allname = "name of all nodes"
